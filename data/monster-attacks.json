{
  "name": "({{Rarity}}) {{Name}}",
  "type": "feat",
  "img": "{{Icon}}",
  "flags": {
    "core": {
      "sheetClass": "foundry-5e-monster-maker-v10.ActionSheet"
    }
  },
  "data": {
    "description": {
      "value": "{{Description}}"
    },
    "activation": {
      "type": "{{Action}}"
    },
    "duration": {
      "value": "{{DurationQuantity}}",
      "units": "{{DurationType}}"
    },
    "target": {
      "value": "{{TargetSize}}",
      "width": "{{TargetWidth}}",
      "units": "{{TargetUnits}}",
      "type": "{{TargetType}}"
    },
    "range": {
      "value": "{{RangeNormal}}",
      "long": "{{RangeLong}}",
      "units": "{{RangeUnits}}"
    },
    "uses": {
      "value": "{{UsesMaximum}}",
      "max": "{{UsesMaximum}}",
      "per": "{{UsesRefresh}}"
    },
    "actionType": "{{Attack}}",
    "damage": {
      "parts": [["{{DmgHit}}", null]]
    },
    "formula": "{{DmgMiss}}",
    "save": {
      "ability": "{{Save}}"
    },
    "requirements": ""
  }
}